# Skill Registry — stock-flow

> Auto-generated by `sdd-init`. Do not edit manually.

## Project Conventions

| File | Purpose |
|------|---------|
| `CLAUDE.md` | Project-level agent instructions (stack, roles, architecture, conventions) |

### Key Conventions (from CLAUDE.md)
- **Stack**: Next.js 15 (App Router) + Payload CMS 3.75 + PostgreSQL + Tailwind 4 + shadcn/ui
- **Architecture**: 3-layer (Client → Action → Service). Server Components by default.
- **No `any`**, no inline schemas, no `index.ts` re-exports, no `console.log` in prod, no comments.
- **Zod schemas** in `src/schemas/[feature]/` with Spanish error messages.
- **Payload collections** in `src/collections/`. Always run `pnpm generate:types` after changes.

## Installed Skills

### SDD Core (skip for skill-resolution)
These are orchestration skills and are excluded from auto-resolution:
- `sdd-init`, `sdd-explore`, `sdd-propose`, `sdd-spec`, `sdd-design`, `sdd-tasks`, `sdd-apply`, `sdd-verify`, `sdd-archive`, `sdd-onboard`

### Reusable Skills

| Skill | Trigger | Description |
|-------|---------|-------------|
| `branch-pr` | Creating a pull request, opening a PR, preparing changes for review. | PR creation workflow for Agent Teams Lite following the issue-first enforcement system. |
| `chained-pr` | When a PR would exceed 400 changed lines, when planning chained PRs, stacked PRs, or reviewable slices. | Split large changes into chained or stacked pull requests that protect reviewer focus and stay within Gentle AI's 400-line cognitive review budget. |
| `cognitive-doc-design` | When writing guides, READMEs, RFCs, onboarding docs, architecture docs, or review-facing documentation. | Design documentation that reduces reader cognitive load through progressive disclosure, chunking, signposting, tables, checklists, and recognition over recall. |
| `comment-writer` | When drafting or posting feedback, review comments, maintainer replies, Slack messages, or GitHub comments. | Write warm, direct, human comments for PRs, issues, reviews, chats, and async collaboration. |
| `go-testing` | When writing Go tests, using teatest, or adding test coverage. | Go testing patterns for Gentleman.Dots, including Bubbletea TUI testing. |
| `issue-creation` | When creating a GitHub issue, reporting a bug, or requesting a feature. | Issue creation workflow for Agent Teams Lite following the issue-first enforcement system. |
| `judgment-day` | When user says "judgment day", "judgment-day", "review adversarial", "dual review", "doble review", "juzgar", "que lo juzguen". | Parallel adversarial review protocol that launches two independent blind judge sub-agents simultaneously to review the same target. |
| `skill-creator` | When user asks to create a new skill, add agent instructions, or document patterns for AI. | Creates new AI agent skills following the Agent Skills spec. |
| `work-unit-commits` | When implementing a change, preparing commits, splitting PRs, or planning chained or stacked PRs. | Structure commits as deliverable work units instead of file-type batches, with tests and docs kept beside the code they verify. |

## Compact Rules for stock-flow

- **TypeScript / React / Next.js**: Use Server Components by default. `'use client'` only for state/events/browser APIs. Always use `useAction` hook; never call actions directly. Use `cn()` from `@/lib/utils` for Tailwind conditionals.
- **Payload CMS**: After modifying collections run `pnpm generate:types`. Migrations live in `src/migrations/`. Collections in `src/collections/`. Security goes in Action layer; services use `overrideAccess: true`.
- **Zod / Forms**: Export inferred type `export type FooValues = z.infer<typeof fooSchema>`. Use `required_error`, `invalid_type_error`, `.trim()`, `.max()`, Spanish messages.
- **Lint / Format**: `pnpm lint`, `pnpm lint:fix`. Prettier config embedded in ESLint rules (printWidth 120, singleQuote, trailingComma all).
- **No tests**: The project has **no test runner** installed. Strict TDD Mode is **disabled**.
